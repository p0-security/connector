{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  // Each example under `examples/` is a standalone consumer with its own
  // tsconfig, not part of this package's build.
  "exclude": ["dist", "examples"]
}
